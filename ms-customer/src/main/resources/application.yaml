spring:
  datasource:
    url: jdbc:h2:mem:ms-customerdb
    username: root
    password: root
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  application:
    name: ms-customer
server:
  port: 8080
logging:
  level:
    org.modelmapper: DEBUG